import { Challenge } from './challenge';
export class Challenge0 implements Challenge {
  day = 0;

  solve(input: string): string {
    try {
      throw new Error("Method not implemented.");
    } catch(error) {
      //console.log(error);
      throw error;
    }
  }

  solve2(input: string): string {
    try {
      throw new Error("Method not implemented.");
    } catch(error) {
      //console.log(error);
      throw error;
    }
  }

}